\documentclass[a4paper,12pt]{article}
\usepackage{cmap}
\usepackage[cp1251]{inputenc}
\usepackage[english, ukrainian, russian]{babel}
\usepackage[left=2cm,right=1.5cm,top=2cm,bottom=2cm]{geometry}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{multicol}
\usepackage{blindtext}

\begin{document}

\pagenumbering{gobble}

\begin{normalsize}

\begin{center}
\section*{Основи математичної логіки.}
\end{center}

\begin{enumerate}

\item Перевірити коректність логічного міркування: Якщо Андрій поїде до Харкова, то Віктор поїде до Києва. Андрій поїде в Харків або в Одесу. Якщо Андрій поїде в Одесу, то Ольга залишиться у Львові. Однак Ольга не залишилась у Львові. Отже, Віктор поїде до Києва.


\item Побудуйте логічну таблицю для наступних формул. Чи є вони тавтологіями, суперечностями, чи нейтральними? Зведіть їх до ДНФ за допомогою еквівалентних перетворень. Побудуйте ВБДР (впорядковану бінарну діаграму розв'язків) або логічну схему. \\
а) $\neg x \vee \neg y \vee (x\wedge y) $; б) $(x\rightarrow y)\rightarrow (y\rightarrow x)$; в) $x\rightarrow (y\rightarrow x)$; \\
г) $(x\rightarrow \neg(y\wedge z)) \rightarrow(z \rightarrow \neg x) $; ґ) $\neg y \wedge x \wedge (x\rightarrow y)$; д) $(x\vee \neg x) \rightarrow (y\wedge \neg y)$; \\
е) $(x\rightarrow (x\rightarrow y))\rightarrow (y\sim x) $; є) $(x\rightarrow y)\wedge (y\rightarrow z) $; \\ 
ж) $(x \rightarrow(y \rightarrow z))\wedge (x\rightarrow y)\wedge (x\wedge \neg z) $; \\
з) $(x\wedge y)\rightarrow z $; и) $((x\wedge y)\rightarrow z)\rightarrow((x\rightarrow z) \wedge(y\rightarrow z)) $; \\
і) $ (y\rightarrow (x\rightarrow (y\rightarrow z)))\rightarrow (x\rightarrow z)$;



\end{enumerate}

\vspace*{2.0cm}

\noindent\rule{\textwidth}{1pt}

\vspace*{1.0cm}


\begin{center}
\section*{Основи математичної логіки.}
\end{center}

\begin{enumerate}

\item Перевірити коректність логічного міркування: Якщо Андрій поїде до Харкова, то Віктор поїде до Києва. Андрій поїде в Харків або в Одесу. Якщо Андрій поїде в Одесу, то Ольга залишиться у Львові. Однак Ольга не залишилась у Львові. Отже, Віктор поїде до Києва.


\item Побудуйте логічну таблицю для наступних формул. Чи є вони тавтологіями, суперечностями, чи нейтральними? Зведіть їх до ДНФ за допомогою еквівалентних перетворень. Побудуйте ВБДР (впорядковану бінарну діаграму розв'язків) або логічну схему. \\
а) $\neg x \vee \neg y \vee (x\wedge y) $; б) $(x\rightarrow y)\rightarrow (y\rightarrow x)$; в) $x\rightarrow (y\rightarrow x)$; \\
г) $(x\rightarrow \neg(y\wedge z)) \rightarrow(z \rightarrow \neg x) $; ґ) $\neg y \wedge x \wedge (x\rightarrow y)$; д) $(x\vee \neg x) \rightarrow (y\wedge \neg y)$; \\
е) $(x\rightarrow (x\rightarrow y))\rightarrow (y\sim x) $; є) $(x\rightarrow y)\wedge (y\rightarrow z) $; \\
ж) $(x \rightarrow(y \rightarrow z))\wedge (x\rightarrow y)\wedge (x\wedge \neg z) $; \\
з) $(x\wedge y)\rightarrow z $; и) $((x\wedge y)\rightarrow z)\rightarrow((x\rightarrow z) \wedge(y\rightarrow z)) $; \\
і) $ (y\rightarrow (x\rightarrow (y\rightarrow z)))\rightarrow (x\rightarrow z)$;
\end{enumerate}


\end{normalsize}
\end{document}

