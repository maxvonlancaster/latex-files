\documentclass[a5paper,10pt]{report}
\usepackage[cp1251]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[english,russian,ukrainian]{babel}
\usepackage{amssymb,head_num}
\usepackage{graphics}
\usepackage{verbatim}
\usepackage[dvipdfm]{graphicx}
\usepackage{bezier, psfig, epsfig, graphics, graphicx}

\setlength{\voffset}{-30mm}
\setlength{\hoffset}{-10mm}
%\sloppy \setlength{\voffset}{-15mm} \setlength{\hoffset}{-15mm}
\setlength{\textwidth}{115mm}
%\setlength{\textheight}{165mm}               % 170   115
\setlength{\textheight}{175mm}               % 240   165
%\renewcommand{\baselinestretch}{.98} %для теорем ариелем
\renewcommand{\baselinestretch}{1}  %для теорем курсивом

\setlength{\headsep}{5mm}


\parskip=1mm

\tolerance=10000

\begin{document}



\begin{titlepage}\normalsize

\begin{center}
Міністерство освіти і науки України

Чернівецький національний університет імені Юрія Федьковича



\vfill

{\bf Мельник Василь Сергійович }
\end{center}

\vfill \rightline{\bf УДК 517.51}

\vfill
\begin{center}
{\LARGE\bf Рівномірні відстані до різних
функціональних класів та
розвиток теореми Гана про
проміжні функції}

\vfill 01.01.01 -- математичний аналіз

\vfill А В Т О Р Е Ф Е Р А Т
\vspace{-18mm}

\vfill
дисертації на здобуття наукового ступеня\\
кандидата фізико-математичних наук
\end{center}



\vfill \centerline{Чернівці -- 2 0 1 8}

\newpage\pagestyle{empty}\normalsize

\noindent \emph{Дисертацією є рукопис.} \vfill

\noindent Робота виконана на кафедрі математичного аналізу
Чернівецького національного університету імені Юрія Федьковича
\mbox{Міністерства} освіти і науки України. \vfill

\noindent \textbf{Науковий керівник}

\vspace{1mm}
\quad доктор фізико-математичних наук, професор\vspace{-1mm}

\quad {\textbf{Маслюченко Володимир Кирилович}},\vspace{-1mm}

\quad завідувач кафедри математичного аналізу \vspace{-1mm}

\quad Чернівецького національного університету\vspace{-1mm}

\quad імені Юрія Федьковича.\vfill



\noindent \textbf{Офіційні опоненти:}

\vspace{1mm}
\quad доктор фізико-математичних наук, професор\vspace{-1mm}

\quad {\textbf{Скасків Олег Богданович}},\vspace{-1mm}

\quad професор кафедри теорії функцій і теорії ймовірностей,\vspace{-1mm}

\quad Львівського національного університету імені Івана Франка;


\vspace{1mm}
\quad кандидат фізико-математичних наук, доцент\vspace{-1mm}

\quad {\textbf{Нестеренко Олексій Никифорович}},\vspace{-1mm}

\quad доцент кафедри математичного аналізу\vspace{-1mm}

\quad  Київського національного університету \vspace{-1mm}

\quad імені Тараса Шевченка.




\vfill

\noindent Захист відбудеться  1 лютого 2019 р. о \quad\quad год. на
засіданні спеціалізованої вченої ради К~76.051.02 у Чернівецькому
національному університеті імені Юрія Федьковича
 за адресою: 58012, м. Чернівці, вул.
Університетська, 28, ауд. 39.\vfill

\noindent З дисертацією можна ознайомитись у бібліотеці
Чернівецького національного університету імені Юрія Федьковича за
адресою: м. Чернівці, вул. Л.~Українки, 23.\vfill

\noindent Автореферат розісланий \hspace{2cm} грудня 2018 р.\vspace{0.2cm}

\noindent
Вчений секретар
\vspace{-1mm}

\noindent спеціалізованої вченої ради\qquad\qquad\qquad\qquad\qquad\qquad\ \ \ \ \ \ Я.~Й.~Бігун

\end{titlepage}






\setcounter{page}{1}
%\setcounter{tocdepth}{1}
%\large
%\input Title.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Теореми i доведення %%%%%%%%%%%%%%%%%%%%%%%%%%
\newcommand{\proclim}[2]{{\bf\sffamily #1} {\itshape #2}}
%\newcommand{\proclim}[2]{{\bf\sffamily #1} {\sffamily #2}}
\newcommand{\proof}[1]{{\bf \sffamily Доведення.} #1\hfill$\diamondsuit$}
\newcommand{\eop}{\hfill$\diamondsuit$}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Грецькi букви %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\def\a{\alpha}       \def\b{\beta}   \def\g{\gamma}   \def\d{\delta}
\def\e{\varepsilon}  \def\f{\varphi} \def\w{\omega}   \def\s{\sigma}
\def\x{\xi}          \def\y{\eta}    \def\z{\zeta}    \def\c{\chi}
\def\l{\lambda}      \def\m{\mu}     \def\n{\nu}      \def\k{\varkappa}
\def\r{\varrho}      \def\t{\tau}    \def\i{\iota}    \def\p{\psi}
\def\th{\theta}
\def\G{\Gamma}       \def\D{\Delta}  \def\F{\Phi}     \def\W{\Omega}
\def\SS{\Sigma}       \def\L{\Lambda} \def\P{\Psi}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Писанi букви %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\def\ca{{\cal A}} \def\cb{{\cal B}} \def\cc{{\cal C}} \def\cd{{\cal D}} \def\cy{{\cal Y}}
\def\cf{{\cal F}} \def\ch{{\cal H}} \def\cu{{\cal U}} \def\cv{{\cal V}}
\def\cw{{\cal W}} \def\cl{{\cal L}} \def\ct{{\cal T}} \def\cs{{\cal S}}
\def\cg{{\cal G}} \def\ck{{\cal K}} \def\cm{{\cal M}} \def\cp{{\cal P}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Потовщенi букви %%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\def\br{{\mathbb R}} \def\bc{{\mathbb C}} \def\bn{{\mathbb N}}
\def\bz{{\mathbb Z}} \def\bd{{\mathbb D}} \def\bi{{\mathbb I}}
\def\bq{{\mathbb Q}} \def\obr{\ov{{\mathbb R}}}
\def\bs{{\mathbb S}} \def\bp{{\mathbb P}} \def\bl{{\mathbb L}}
\def\bb{{\mathbb B}}  \def\ba{{\mathbb A}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Операцiї та функцiї %%%%%%%%%%%%%%%%%%%%%%%%%%%
\def\Cup{\bigcup\limits}    \def\Cap{\bigcap\limits}    \def\iy{\infty}
\def\Prod{\prod\limits}     \def\Sum{\sum\limits}
\def\<{\subseteq}           \def\>{\supseteq}           \def\*{\times}
\def\ov{\overline}          \def\Int{{\rm int}}       \def\Cl{{\rm cl}}
\def\pr{{\rm pr}}         \def\copr{{\rm copr}}     \def\dim{{\rm dim}}
\def\fr{{\rm fr}}         \def\ext{{\rm ext}}       \def\st{{\rm st}}
\def\os{\stackrel{\underline{\phantom{\subset}}}{\subset}}
\def\diam{{\rm diam}}     \def\span{{\rm span}}     \def\co{{\rm co}}
\def\absco{{\rm absco}} \def\sup{\mathop{\rm sup}\limits}
\def\inf{\mathop{\rm inf}\limits}  \def\lim{\mathop{\rm lim}\limits}
\def\Lim{\mathop{\rm Lim}\limits}  \def\stm{\setminus}  \def\xp{{\rm xp}}
\def\toto{\stackrel{\longrightarrow}{\scriptstyle\longrightarrow}}
\def\lra{\Longleftrightarrow}    \def\la{\Leftarrow}  \def\ra{\Rightarrow}
\def\wg{\wedge}
\def\const{{\rm const}}
\def\id{{\rm id}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Iнше %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\def\dis{\displaystyle}
\def\~{\tilde}
\def\wt{\widetilde}
\def\#{\prec}
\def\ua{U^\alpha}
\def\ub{U^\beta}
\def\va{V^\alpha}
\def\vb{V^\beta}
\def\lt{\ell_\iy(T)}

\centerline{\bf ЗАГАЛЬНА ХАРАКТЕРИСТИКА РОБОТИ}

\vspace{0.3cm}


\textbf{Актуальність теми.} У 1917 році відомий австрійський математик Ганс Ган встановив такий результат: якщо $g:X\longrightarrow \mathbb{R}$ і $h:X\longrightarrow \mathbb{R}$ $-$ напівнеперервні відповідно зверху і знизу функції, що задані на метричному просторі $X$ і такі, що $g(x)\leq h(x)$ на $X$, то існує неперервна функція $f:X\longrightarrow \mathbb{R}$, така, що $g(x)\leq f(x)\leq h(x)$ на $X$. Пару $(g,h)$ двох напівнеперервних відповідно зверху і знизу функцій на топологічному просторі $X$, таких, що $g(x)\leq h(x)$ /$g(x)< h(x)$ / на $X$ ми називаємо /строгою/ парою Гана. А функцію $f$, для якої виконується нерівність $g(x)\leq f(x)\leq h(x)$ /$g(x)< f(x)< h(x)$, якщо $g(x)< h(x)$, і $g(x)= f(x)= h(x)$, якщо $g(x)= h(x)$/ називають /строго/ проміжною функцією для пари Гана $(g,h)$. Тому сформульований результат Гана і його узагальнення та аналоги часто називають теоремами про проміжну функцію. У 1944 році Ж. Д'єдонне переніс теорему Гана на випадок паракомпактних просторів. А трохи пізніше Ґ. Тонґ  і М. Катетов з'ясували, що існування проміжної неперервної функції для кожної пари Гана на $X$ є характеристичною умовою нормальності в класі $T_{1}$-просторів.

К.Г. Даукер і М. Катетов для функцій $g$ і $h$, таких, що $g(x)< h(x)$ на $X$, цікавилися побудовую неперервної строго проміжної функції $f:X\longrightarrow \mathbb{R}$ для $T_{1}$-простору $X$. Виявляється, що для цього необхідною і достатньою умовою є нормальність і зліченна паракомпактність простору $X$.

Е. Майкл будував строго проміжну функцію $f$ для довільної пари Гана на просторі $X$. Для її існування необхідно і достатньо, щоб $X$ був досконально нормальним.

Нові підходи до доведення цих результатів виявили К. Ґуд і \\Я. Старс.

Недавно у працях К. Ямазакі та інших авторів вивчалася можливість перенесення теореми Гана про проміжну функцію на випадок відображень зі значеннями у банахових ґратках. Тут були отримані як позитивні результати для сепарабельних і несепарабельних банахових ґраток, що стосується теорем Даукера-Катетова і Майкла, і ґраток $l_{p}$ ($1\leq p<\infty$) і $c_{0}$, що стосується теореми Катетова-Тонґа, так і негативні результати щодо останньої теореми для ґратки $c$.

Ці дослідження ще не завершені і актуально вивчити можливість перенесення теореми Гана про проміжну функцію на відображення зі значеннями у різних впорядкованих топологічних векторних просторах, наприклад, у просторі $C_{p}(T)$ всіх неперервних функцій з топологією поточкової збіжності. У цьому останньому випадку фактично йдеться про отримання теореми про проміжну функцію для нарізно неперервних функцій.

В. Маслюченко і С. Петей у 2015 році знайшли інший аналог теореми Гана про проміжну функцію: для довільних зростаючих функцій $g,h:[a,b]\longrightarrow \mathbb{R}$, для яких $g(x)\leq h(x)$ на $[a,b]$ і $g$ $-$ напівнеперервна зверху, а $h$ $-$ знизу, існує така зростаюча неперервна функція $f:[a,b]\longrightarrow \mathbb{R}$, що $g(x)\leq f(x)\leq h(x)$ на $[a,b]$. Цей результат відкрив нові можливості розвитку теореми Гана, а саме, виникло питання: для яких ще властивостей функцій, крім їх зростання, справджується аналог теореми про проміжну функцію?

Так само цікаво було б з'ясувати: чи є аналоги теореми Гана для многозначних відображень.

Теорема про проміжну функцію застосовується для знаходження рівномірної відстані $d(f,C(X))$ до простору $C(X)$ всіх неперервних функцій $g:X\longrightarrow \mathbb{R}$. Це частково було зроблено ще Г. Ганом для метричного простору $X$. Потім Й. Бен'яміні і Й. Лінденштраусс довели, що $$d(f,C_{b}(X))=\frac{1}{2}||\omega_{f}||,$$ де $X$ $-$ паракомпактний простір, $f:X\longrightarrow \mathbb{R}$ $-$ обмежена функція, $C_{b}(X)$ простір всіх обмежених неперервних функцій $g:X\longrightarrow \mathbb{R}$, $\omega_{f}$ $-$ коливання функції $f$ і $||\cdot||$ $-$ рівномірна норма.

Втім, теорема про проміжну функцію має місце для нормальних просторів, і тому постало природне питання про рівномірну відстань $d(f,C(X))$, де $C(X)$ $-$ простір усіх неперервних функцій $g:X\longrightarrow \mathbb{R}$ для нормального простору $X$.

В теорії функцій у XX столітті з'явилося багато аналогів неперервності $-$ квазінеперервність, ледь неперервність, майже неперервність і т.д. Відносно кожного з цих аналогів неперервності природно постає питання про рівномірну відстань до відповідного функціонального класу, зокрема, для випадку квазінеперервних функцій.





\textbf{Зв’язок з науковими програмами, планами, темами.} Тематика
досліджень пов’язана з науково-дослідними програмами ``Топологічні
аспекти теорії функцій та регулярні оператори на банахових ґратках''
(номер держ. реєстрації -- 0112U002340 ) і ``Різні класи операторів
в абстрактних просторах'' (номер держ. реєстрації -- 0110U001560).

\textbf{Мета і задачі дослідження.}

\emph{Метою} роботи є дослідження можливості знаходження проміжних функцій для різних класів функцій та різних просторів їх задання, знаходження відстаней до різних функціональних класів.

\emph{Задачами дослідження} є:

 - дослідження можливості перенесення результатів Гана на інші типи функцій (функції обмеженої варіації, диференційовні функції);


 - дослідження можливості знаходження проміжних функцій з тих чи інших функціональних класів з фіксованими значеннями;

 - знаходження рівномірної відстані до різних функціональх класів, зокрема, до класу квазінеперервних функцій в точці.




\emph{Об’єктом} досліджень є проміжні функції та рівномірна відстань до різних функціональних класів.

\emph{Предметом} дослідження є аналоги теореми Гана про проміжну функцію, знаходження рівномірної відстані до різних функціональних класів.

\textbf{Методи дослідження.} В дослідженні застосовуються \emph{методи} загальної теорії функцій, загальної топології та теорії наближень.

\textbf{Наукова новизна одержаних результатів}. У дисертацiйнiй
роботi отримано наступні нові наукові результати:

-- досліджено питання про існування проміжної афінної функції для пари функцій $(g,h)$, де $g$~--- вгнута, $h$~--- опукла, та $g(x)\leq h(x)$ на векторному просторі $X$;

 -- доведено існування строго проміжних проміжних кусково лінійної чи нескінченно диференційовної функції з даним значенням для строгої пари Гана $(g,h)$ на відрізку;

 -- доведено існування строго проміжної нескінченно диференційовної функції з даним значенням для строгої пари Гана на замкненому паралелепіпеді в $\mathbb{R}^{n}$;

 -- з допомогою диференційовних розбиттів одиниці побудовані строго проміжні нескінченно диференційовні функції та диференційовні функції на гільбертових та асплундових просторах відповідно;

 -- отримано перші аналоги теореми Гана для многозначних відображень з топологічного простору $X$ в $\mathbb{R}$;


 -- введено поняття нарізної пари Гана, та доведено, що для $T_{1}$-просторів $X$ та $Y$ кожна нарізна пара Гана $(g,h)$ на добутку $X\times Y$ має проміжну нарізно неперервну функцію тоді і тільки тоді, коли добуток $X\times Y$ з хрест-топологією є нормальним;


-- з допомогою теореми Гана про проміжну функцію розв`язана нульова обернена задача з теорії наближень;

-- доведено, що для нормального простору $X$ рівномірна відстань від функції $f$ до множини $C(X)$ всіх неперервних функцій дорівнює половині рівномірної норми коливання $\omega_{f}$ функції $f$, що є розвитком результату Бен'яміні-Лінденштраусса;

-- з допомогою поняття граничної множини знайдено відстань до простору квазінеперервних в неізольованій точці $x_{0}$ функцій, заданих на топологічному просторі $X$ з першою аксіомою зліченності для обмеженої функції $f$ з множиною точок розриву $D(f)=\{x_{0}\}$ або ${\O}$.


\textbf{Наукове та практичне значення досліджень.} Дослідження
носять теоретичний характер. Їх результати можуть бути використані в теорії функцій, зокрема, теорії наближень і в функціональному аналізі.


\textbf{Особистий внесок здобувача.} Всі результати, наведені в
дисертації, отримані здобувачем особисто. Постановки задач і
вказівки щодо вибору методів досліджень належать науковому керівнику
В.К.~Маслюченку. О.В.~Маслюченко вказав, що при побудові строго проміжної диференційовної функції можна використати теорему Даукера-Катетова,
В.В. Конаровський порадив при побудові проміжної функцій обмеженої варіації використати прийом мінімізації. Г. Волошин брала участь в обговоренні результатів статті [5] і допомогала при її оформленні.

%, а O.І.~Філіпчук -- у обговоренні результатів праці~\cite{MMF_TopolAppl}.



\textbf{Апробацiя результатiв дисертацiї.} Результати дисертації
були апробовані на таких конференціях, школах і семінарах:


 -- ``IV Міжнародна Ганська конференція'' в
 Чернівцях;

 -- конференція, присвячена 100-річчю від дня народження К.М. Фішмана та М.К. Фаге;


 -- конференція пам'яті Стефана Банаха у Львові;

 -- міжнародна конференця "Теорія наближення функцій та її застосування" присвячена 75-річчю з дня народження члена-кореспондента НАН України, професора О.І. Степанця у Слов'янську;


  -- літня школа з еволюційного обчислення в оптимізації та дата майнингу в Ясах, Румунія;

  -- ``Комплексний аналіз, теорія потенціалу та її застосування''  у Києві;


  -- міжнародна конференція до 120-ліття з дня народження Казимира Куратовського у Львові;

   -- Всеукраїнські  наукові конференції ``Сучасні проблеми теорії ймовірностей та математичного аналізу''
  у Ворохті;

  -- на XII  Літній школі ``Алгебра, топологія і аналіз'' в Колочаві;

 -- міжвузівський науковий семінар "Прикладні задачі та ІТ-технології", присвячений 100-річчю з дня народження професора Василя Павловича Рубаника і 55-річчю кафедри прикладної математики та інформаційних технологій.

 Крім того, вони
неодноразово доповідалися на наукових семінарах кафедри
математичного аналізу Чернівецького національного університету імені
Юрія Федьковича (керівник -- проф. В.К.~Маслюченко) та на семінарі
факультету математики та інформатики Чернівецького національного
університету імені Юрія Федьковича (керівник -- проф. І.М.~Черевко).




\textbf{Публiкацiї.} За результатами дисертаційних досліджень
опубліковано 20 наукових праць, з них 6 статтей у наукових фахових
виданнях з математики і 14~-- у матеріалах і тезах конференцій. Серед статтей дві опубліковані у виданнях, що входять до міжнародних науково-метричних баз.



\textbf{Структура та обсяг роботи.} Дисертація складається зі
вступу, п'яти розділів, висновків, списку використаних джерел, що
містить 109 найменувань на 9 сторінках та списку термінів та умовних
позначень. Повний обсяг роботи -- 111 сторінoк.

\emph{Здобувач висловлює щиру подяку науковому керівнику професору Маслюченку В.К. за терпіння, розуміння, підтримку і допомогу в роботі над дисертацією.}


\vspace{0.7cm}

\centerline{\bf ОСНОВНИЙ ЗМICТ РОБОТИ}

\vspace{0.3cm}
При нумерації результатів дисертації перше число означає номер розділу, друге -- підрозділу, третє -- номер теореми в підрозділі.

У \textbf{вступі} дисертації розкрито основну суть досліджуваних проблем
і їх історію, обґрунтовано
актуальність теми дисертації, сформульовано мету і задачі
дослідження та виділено основні результати дисертації.

У \textbf{першому розділі} зроблено огляд публікацій, що стосуються
тематики дисертаційної роботи та наведено результати, які використовуються в дисертації.


У \textbf{другому розділі} дано огляд основних результатів дисертації.


У \textbf{третьому розділі} розглянуто пари функцій з спеціальними властивостями та можливість існування для цих пар проміжних функцій з даними властивостями.


Для функцій $g:E\rightarrow \mathbb{R}$ і $h:E\rightarrow \mathbb{R}$ ми будемо використовувати позначення: $$Gr^{-}(g)=\{(x,y):x\in E\quad \textmd{і} \quad y<g(x)\}$$ і $$Gr^{+}(h)=\{(x,y):x\in E\quad \textmd{і} \quad y>h(x)\}.$$

Центральний результат підрозділу 3.1 є аналогом теореми Гана для опуклих та вгнутих функцій.

\textbf{Теорема 3.1.2.}
Нехай $X$ — дійсний векторний простір, $E$ — опукла множина в $X$, $g:E\rightarrow \mathbb{R}$ — вгнута і $h:E\rightarrow \mathbb{R}$ — опукла функції, такі, що $g(x)\leq h(x)$ на $E$, та множини $A=Gr^{-}(g)$ і $B=Gr^{+}(h)$ напівстрого відокремлюються деякою гіперплощиною в добутку $X\times\mathbb{R}$. Тоді існує така афінна функція $f:E\rightarrow \mathbb{R}$, що $g(x)\leq f(x)\leq h(x)$ на $E$.

Далі, використовуючи теорему Мазура, ми встановили наступну теорему.

\textbf{Теорема 3.1.8.}
Нехай $X$~--- дійсний векторний топологічний простір, $g:X\rightarrow \mathbb{R}$ — вгнута функція, $h:X\rightarrow \mathbb{R}$ — опукла функція, такі, що $g(x)\leq h(x)$ на $E$, і одна з множин $A=Gr^{-}(g)$ чи $B=Gr^{+}(h)$ має $C$-внутрішню точку в добутку $X\times \mathbb{R}$. Тоді існує така афінна функція $f:X\rightarrow \mathbb{R}$, що $g(x)\leq f(x)\leq h(x)$ на $X$.

\textbf{Означення.} Пару $(g,h)$ напівнеперервних відповідно зверху і знизу функцій $g,h:X\rightarrow \mathbb{R}$ на топологічному просторі $X$, таких, що $g(x)\leq h(x)$ на $X$ ми називаємо \textit{парою Гана} на $X$. Якщо ж $g(x)<h(x)$ на $X$, то пара Гана називається \textit{строгою}. Функція $f:X\rightarrow \mathbb{R}$, для якої $g(x)\leq f(x)\leq h(x)$ на $X$ називається \textit{проміжною} для пари $(g,h)$, а коли $g(x)<f(x)<h(x)$ при $g(x)<h(x)$ і $g(x)=f(x)=h(x)$ при $g(x)=h(x)$, то~--- \textit{строго проміжною}.


Нехай $C_{u}[a,b]$~--- банаховий простір всіх неперервних функцій\\ $f:[a,b]\rightarrow\mathbb{R} $ з рівномірною нормою $||f||=\max\limits_{x\in[a,b]}^{}|f(x)|$. З теореми Даукера-Катетова легко виводиться такий результат.

\textbf{Теорема 3.2.1.}
Нехай $E$~--- всюди щільна множина у просторі $C_{u}[a,b]$ і $(g,h)$~--- строга пара Гана на $[a,b]$. Тоді існує строго проміжна для $(g,h)$ функція $f$ з множини $E$.

З цієї теореми безпосередньо випливає, що для кожної строгої пари Гана на відрізку $[a,b]$ можна побудувати многочлен чи кусково лінійну функцію, які є строго проміжними для $(g,h)$. Зауважимо, що для строгих пар Гана на $\mathbb{R}$ метод побудови строго проміжних $C^{\infty}$-функцій чи кусково лінійних функцій з теореми 3.2.1 не застосовний. Ми розвинули інші методи, які дозволяють будувати строго проміжні $C^{\infty}$-функції і кусково лінійні функції з певними додатковими властивостями для строгих пар Гана на проміжках числової прямої, не використовуючи при цьому теорему Даукера-Катетова, а безпосередньо доводячи її підсилені версії. Ці результати дозволяють здійснити побудову строго проміжних $C^{\infty}$-функцій і кусково лінійних функцій, зокрема, на всій числовій прямій $\mathbb{R}$. Отримані в цьому напрямку результати викладені в підрозділах 3.2 та 3.3.

\textbf{Означення.}
Функцію $f:(a,b)\rightarrow \mathbb{R}$ ми називатимемо кусково лінійною, якщо існує така двостороння послідовність $(x_{n})_{n\in \mathbb{Z}}$ точок $x_{n}$ з інтервалу $(a,b)$, що $x_{n}<x_{n+1}$ для кожного $n\in \mathbb{Z}$, $\lim_{n\rightarrow +\infty}x_{n}=b$,  $\lim_{n\rightarrow -\infty}x_{n}=a$ і звуження $f|_{[x_{n},x_{n+1}]}$ є лінійною функцією для кожного $n\in \mathbb{Z}$.

\textbf{Теорема 3.2.2.}
Нехай $(g,h)$~--- строга пара Гана на відрізку $I=[a,b]$ і $g(a)<\gamma<h(a)$. Тоді існує кусково лінійна функція $f:I\rightarrow \mathbb{R}$, така, що $g(x)<f(x)<h(x)$ на $I$ та $f(a)=\gamma$.

Точку $a$  в цій теоремі можна замінити на $b$.

\textbf{Теорема 3.3.4.}
Нехай $I=(a,b)$ довільний інтервал в $\mathbb{R}$, скінченний чи нескінченний, $x_{0}\in I$, $(g,h)$~--- строга пара Гана на $I$, і $g(x_{0})<y_{0}<h(x_{0})$. Тоді існує строго проміжна для пари $(g,h)$ кусково лінійна функція $f:I\rightarrow \mathbb{R}$, така, що $f(x_{0})=y_{0}$.

У підрозділі 3.3 конструюються проміжні нескінченно диференційовні функції на проміжках числової прямої з даними значеннями. Там доводяться такі теореми:

\textbf{Теорема 3.3.1.}
Нехай $(g,h)$~--- строга пара Гана на $I=[a,b]$, і $\gamma$~--- довільне число з інтервалу $(g(a),h(a))$. Тоді для пари $(g,h)$ існує строго проміжна $C^{\infty}$-функція $f:I\rightarrow \mathbb{R}$, така, що $f$ локально стала в точках $a$ і $b$ і $f(a)=\gamma$.

\textbf{Теорема 3.3.2.}
Нехай $(g,h)$~--- строга пара Гана на $I=[a,b]$, і $\gamma$~--- довільне число з інтервалу $(g(b),h(b))$. Тоді для пари $(g,h)$ існує строго проміжна $C^{\infty}$-функція $f:I\rightarrow \mathbb{R}$, така, що $f$ локально стала в точках $a$ і $b$ і $f(b)=\gamma$.

\textbf{Теорема 3.3.3.}
Нехай $I=(a,b)$ довільний інтервал в $\mathbb{R}$, скінченний чи нескінченний, $x_{0}\in I$, $(g,h)$~--- строга пара Гана на $I$, і $g(x_{0})<y_{0}<h(x_{0})$. Тоді існує строго проміжна для пари $(g,h)$ нескінченно диференційовна функція $f:I\rightarrow \mathbb{R}$, така, що $f(x_{0})=y_{0}$.

Тут у міркуваннях використовується компактність відрізка $[a,b]$, як це робилося і у підрозділі 3.2. Ідеї, застосовані у підрозділах 3.2 і 3.3, розвиваються у підрозділі 3.4 для функцій багатьох змінних.

Перший підхід базується на згаданій у вступі теоремі Даукера-Катетова. Нехай $X$~--- компактний гаусдорфовий простір (коротко: компакт). Символом $C_{u}(X)$ позначимо банаховий простір всіх неперервних функцій $f:X\rightarrow \mathbb{R}$ з рівномірною нормою $||f||=\max\limits_{x\in X}^{}|f(x)|$. Зокрема, в ролі $X$ може виступати замкнений паралелепіпед в $\mathbb{R}^{n}$.

\textbf{Теорема 3.4.1.}
Нехай $X$~--- компакт, $L$~--- всюди щільна множина у просторі $C_{u}(X)$ і $(g,h)$~--- строга пара Гана на $X$. Тоді існує строго проміжна для $(g,h)$ функція $f:X\rightarrow \mathbb{R}$, така, що $f\in L$.

Позначимо символом $P(X)$ простір всіх многочленів $$g(\xi_{1},...,\xi_{n})=\sum\limits_{k_{1},...,k_{n}=0}^{m}a_{k_{1},...,k_{n}}\xi_{1}^{k_{1}}...\xi_{n}^{k_{n}}$$ від $n$ змінних на замкненому паралелепіпеді $X$ у просторі $\mathbb{R}^{n}$. Добре відомо, що $P(X)$ для замкненого паралелепіпеда $X$ в $\mathbb{R}^{n}$~--- це всюди щільний підпростір банахового простору $C_{u}(X)$ всіх неперервних функцій $f:X\rightarrow \mathbb{R}$ з рівномірною нормою. Тому з теореми 3.4.1 негайно випливає

\textbf{Теорема 3.4.2.}
Нехай $X=[a_{1},b_{1}]\times...\times[a_{n},b_{n}]$~--- це замкнений паралелепіпед в $\mathbb{R}^{n}$ і $(g,h)$~--- строга пара Гана на $X$. Тоді існує такий многочлен  $f\in P(X)$, що є строго проміжною функцією для пари $(g,h)$.

Застосовуючи інший прямий метод, що не базується на теоремі Даукера-Катетова, доводиться такий результат.

\textbf{Теорема 3.4.3.}
Нехай $X=[a_{1},b_{1}]\times...\times[a_{n},b_{n}]$~--- замкнений паралелепіпед в $\mathbb{R}^{n}$, $(g,h)$~--- строга пара Гана на $X$, $x_{0}\in X$ і $g(x_{0})<y_{0}<h(x_{0})$. Тоді існує така строго проміжна для пари $(g,h)$ нескінченно диференційовна функція $f:X\rightarrow \mathbb{R}$, що $f(x_{0})=y_{0}$.

У підрозділі 3.5 будуються проміжні диференційовні функції на гільбертових просторах. Основним технічним знаряддям, як і в доведенні теореми 3.4.3, виступають диференційовні розбиття одиниці.

\textbf{Теорема 3.5.1.}
Нехай $X$~--- дійсний сепарабельний гільбертовий простір і $(U_{j})_{j\in J}$~--- відкрите покриття простору $X$. Тоді існує локально скінченне $C^{\infty}$-розбиття одиниці $(\varphi_{i})_{i\in I}$ на $X$, яке підпорядковане цьому покриттю.

З допомогою цієї теореми доводиться

\textbf{Теорема 3.5.2.}
Нехай $X$~--- сепарабельний гільбертовий простір і $(g,h)$~--- строга пара Гана на $X$. Тоді існує $C^{\infty}$-функція $f:X\rightarrow \mathbb{R}$, яка буде строго проміжною для пари $(g,h)$.

В підрозділі 3.6 досліджується можливість перенесення попередніх результатів на загальний випадок нормованих просторів. Це вдалося частково зробити для так званих асплундових просторів. Нагадаємо, що дійсний банаховий простір $X$ називається асплундовим, якщо для довільного його сепарабельного підпростору $L$ спряжений з ним простір $L^{*}$ теж сепарабельний. Можна довести, що кожний сепарабельний банаховий простір $X$ з сепарабельним спряженим $X^{*}$ є асплундовим. Рефлексивні банахові простори є асплундовими, зокрема, такими є простори $l_{p}(I)$ при $1<p<\infty$, простори $c_{0}(I)$ теж асплундові, а простори $l_{1}$ і $C[0,1]$~--- ні.

Можна довести, що для довільного дійсного сепарабельного банахового простору наступні умови еквівалентні:

(i) на $X$ існує диференційовна при $x\neq 0$ норма, що еквівалентна до вихідної норми простору $X$;

(ii) на $X$ існує диференційовна функція з обмеженим непорожнім носієм;

(iii) спряжений з $X$ простір $X^{*}$ сепарабельний.

Основні результати підрозділу 3.6 такі:

\textbf{Теорема 3.6.3.}
Нехай $X$~--- сепарабельний асплундовий простір і $(U_{j})_{j\in J}$~--- його відкрите покриття, що складається з обмежених множин $U_{j}$. Тоді існує локально скінченне розбиття одиниці $(\varphi_{i})_{i\in I}$ на $X$, яке підпорядковане покриттю $(U_{j})_{j\in J}$ і складається з диференційовних функцій $\varphi_{i}$.

\textbf{Теорема 3.6.4.}
Нехай $(g,h)$~--- строга пара Гана на сепарабельному асплундовому просторі $X$. Тоді для неї існує строго проміжна диференційовна функція $f:X\rightarrow \mathbb{R}$.

У \textbf{четвертому} розділі проведено дослідження аналогів теорем Гана для многозначних відображень та нарізно неперервних функцій на добутках топологічних просторів.

У підрозділі 4.1 розглядаються зв'язки між теоремою Гана та існуванням проміжної неперервної многозначної функції для многозначних напівнеперервних відповідно зверху та знизу функцій  $G$ та $H$ на нормальному просторі $X$, таких, що $G(x)\subseteq H(x)$ на $X$. Цю пару многозначних функцій ми називаємо многозначною парою Гана на $X$.

Зокрема, з використанням теореми Гана та властивостей многозначних функцій, було встановлено наступний результат:

\textbf{Теорема 4.1.1.}
Нехай $X$ нормальний простір і $(G,H)$ пара Гана, така, що $G(x)=[g_{1}(x),g_{2}(x)]$ і $H(x)=[h_{1}(x),h_{2}(x)]$ для кожного $x\in X$. Тоді існує проміжна для $(G,H)$ неперервна функція $F:X\rightarrow \mathbb{R}$ така, що $F(x)=[f_{1}(x),f_{2}(x)]$ на $X$, де функції $f_{1}$ і $f_{2}$ є неперервними.

Також було встановлено наступний зв`язок між теоремою Гана і існуванням проміжної многозначної функції для многозначної пари Гана.

\textbf{Теорема 4.1.2.}
Нехай $X$~--- топологічний простір, $(g,h)$~--- пара Гана на $X$, $G(x)=(-\infty,g(x)]$  і $H(x)=(-\infty,g(x)]$, і пара Гана $(G,H)$ має проміжну неперервну функцію $F:X\rightarrow \mathbb{R}$. Тоді пара Гана $(g,h)$ також має проміжну неперервну функцію $f:X\rightarrow \mathbb{R}$.

У підрозділі 4.3 нами було введено поняття нарізної пари Гана на добутку просторів $X$ та $Y$.

\textbf{Означення.} Пара функцій $g,h:X\times Y\rightarrow \mathbb{R}$ називається нарізною парою Гана на $X\times Y$, якщо $g$~--- нарізно напівнеперервна зверху функція, а $h$~--- знизу, та $g(p)\leq h(p)$ на $X\times Y$.

\textbf{Означення.} Нехай $X$ і $Y$~--- топологічні простори. Множина $W\subseteq X\times Y$ називається нарізно відкритою, якщо для кожної точки $p=(x,y)\in W$ існують околи $U$ та $V$ точок $x$ та $y$ у просторах $X$ та $Y$ відповідно, такі, що $$(\{x\}\times V)\cup(U\times \{y\})\subseteq W.$$ Система $\mathcal{C}$, що складається з нарізно відкритих множин, утворює топологію на добутку $X\times Y$, яка називається хрест-топологією на $X\times Y$.

\textbf{Теорема 4.3.1.}
Нехай $X$ та $Y$~--- $T_{1}$-простори. Тоді кожна нарізна пара Гана $(g,h)$ на добутку $X\times Y$ має проміжну нарізно неперервну функцію тоді і тільки тоді, коли простір $Q=(X\times Y, \mathcal{C})$ є нормальним.

Також було встановлено наступний результат, що стосується часткового випадку добутку просторів.

\textbf{Теорема 4.3.2.}
Нехай $T$ та $S$~--- дискретні простори, $X=\alpha T=T\cup\{\infty\}$, та $Y=\alpha S=S\cup\{\infty\}$~--- компактифікації Александрова цих просторів, $\mathcal{C}$~--- хрест-топологія на $X\times Y$ і $Q=(X\times Y, \mathcal{C})$. Тоді $Q$~--- нормальний топологічний простір.

У підрозділі 4.4 виявлено несподіваний звязок пар Гана з теорією нарізно неперервних відображень та теорією наближень.

Для довільної функції $f:X\rightarrow \mathbb{R}$, заданої на множині $X$ розглянемо числа $$M(f)= \sup\limits_{x\in X}^{}f(x)\textmd{, }m(f)=\inf\limits_{x\in X}^{}f(x)\textmd{ і } ||f||=M(|f|).$$
Нехай $X=[a,b]$~--- відрізок числової прямої і $P_{n}[a,b]$~--- лінійний підпростір простору $C[a,b]$, що складається з усіх многочленів $g(x)=\sum\limits_{k=0}^{n}a_{k}x^{k}$, степінь яких не перевищує $n$. Для функції $f\in C[a,b]$ число $$E_{n}(f)=d(f,P_{n}[a,b])=\inf\{||f-g||:g\in P_{n}[a,b]\}$$ називається $n$-им рівномірним наближенням $f$. Зокрема, $E_{0}(f)=\inf\{||f-c||:c\in \mathbb{R}\}$. Оскільки сталі функції можна розглядати на довільний множині $X$, то і число $E_{0}(f)$ визначається цією ж формулою для довільних функцій $f:X\rightarrow \mathbb{R}$, заданих на непорожній множині $X$.

\textbf{Теорема 4.4.1.} Нехай $X$~--- непорожня множина, $f:X\rightarrow \mathbb{R}$~--- обмежена функція і $\mu=\frac{1}{2}(M(f)-m(f))$. Тоді $E_{0}(f)=\mu$, при цьому стала функція $c_{0}=\frac{1}{2}(M(f)+m(f))$ є єдиною серед сталих функцій, для якої виконується рівність $$E_{0}=\mu=||f-c_{0}||.$$

Для відображення $f:X\times Y\rightarrow Z$ і точки $(x,y)\in X\times Y$ покладемо $$f^{x}(y)=f(x,y)=f_{y}(x).$$



\textbf{Теорема 4.4.2.}
Нехай $X$ -- топологічний простір, $Y$ -- непорожня множина, $f: X\times Y \rightarrow \mathbb{R}$ -- неперервна відносно першої змінної і обмежена відносно другої функція, і $g(x)=\inf\limits_{y\in Y}f^x(y)$, a $h(x)=\sup\limits_{y\in Y}f^x(y)$ на $X$. Тоді $(g,h)$ -- це пара Гана на $X$.



\textbf{Наслідок.} Нехай $X$ -- топологічний простір, $Y$ -- непорожній компактний простір, $f: X\times Y \rightarrow \mathbb{R}$ -- нарізно неперервна функція і $g(x)=\min\limits_{y\in Y}f^x(y)$, a $h(x)=\max\limits_{y\in Y}f^x(y)$ на $X$. Тоді $(g,h)$ -- це пара Гана на $X$.

Для довільної функції $f: X\times Y\rightarrow \mathbb{R}$ визначимо функції $S_{f}: X\rightarrow (-\infty, +\infty]$ та $I_{f}: X\rightarrow [-\infty, +\infty)$ формулами

$$S_f(x)=\sup\limits_{y\in Y}f(x,y) \qquad \mbox{та} \qquad I_f(x)=\inf\limits_{y\in Y}f(x,y).$$

Наступний результат одержується методом, застосованим для побудови нарізно неперервної функції з даною діагоналлю, що належить до першого класу Бера. Він отримується з допомогою однієї теореми Тонґа про напівнеперервні функції

\textbf{Теорема 4.4.4.}
Нехай $h: X\rightarrow \mathbb{R}$ -- напівнеперервна знизу функція, що задана на досконало нормальному просторі $X$ з нормальним квадратом $X^2$, в якому діагональ $\Delta=\{(x,x): x\in X\}$ є $G_{\delta}$-множиною, і $h_0: X\rightarrow \mathbb{R}$ -- неперервна функція, для якої $h_0(x)\leq h(x)$ на $X$. Тоді існує така нарізно неперервна функція $f: X^2\rightarrow \mathbb{R}$, що $h_0(x)\leq f(x,y)\leq h(x)$ на $X^2$ i $S_f(x)=h(x)$ на $X$.

Так само будується функція $f$, для якої $I_f(x)=g(x)$ на $X$, де $g$~--- напівнеперервна зверху функція.

Звідси виводиться

\textbf{Теорема 4.4.7.}
Нехай $X=[a, b]$,  $Y=[c, d]$, де $a<b$, $c<d$ i $(g,h)$~--- довільна пара Гана на відрізку  $X$.  Тоді існує така нарізно неперервна функція $f: X\times Y\rightarrow\mathbb{R}$, що $(I_f, S_f)=(g, h)$.


Для нарізно неперервної функції $f:[a,b]\times [c,d]\rightarrow\mathbb{R}$ покладемо $\alpha_f(x)=\frac{M(f^x)-m^{(f^x)}}{2}$. З теореми 4.4.1 випливає, що $\alpha_f(x) = E_{0}(f^{x})$. З теореми 4.4.7 легко випливає наступний результат, який дає розв`язання нульової оберненої задачі для прямокутника.


\textbf{Теорема 4.4.8.}
Для довільного прямокутника $P=X\times Y$, де $X=[a, b]$ i $Y=[c, d]$, і кожної невід'ємної напівнеперервної знизу функції $\varphi: X \rightarrow \mathbb{R}$ існує така нарізно неперервна функція $f: P \rightarrow \mathbb{R}$, що $\alpha_f=\varphi$.

\textbf{П'ятий} розділ дисертації присвячено задачі знаходження відстаней від деяких функцій до просторів функцій з певними властивостями. В підрозділі 5.1 розглянуто деякі приклади, повязані зі знаходженням відстаней до простору неперервних функцій для деяких конкретних функцій першого класу Бера.

Підрозділ 5.2 присвячено знаходженню відстані до простору неперервних функцій $C(X)$.

\textbf{Теорема 5.2.1.}
Нехай $X$~--- нормальний простір, $f:X\to {\mathbb R}$ - довільна функція. Тоді  $$d(f,C(X))=\frac{1}{2} \left\| \omega _{f} \right\|.$$

В доведенні використовується теорема Гана - Д'єдонне - Тонґа - Катетова. Теорема 5.2.1 розвиває відомий результат Бен'яміні-Лінденштраусса, які розглядали випадок паракомпактного простору $X$, а замість $C(X)$ розглядали простір $C_{b}(X)$ всіх обмежених неперервних функцій.

В підрозділі 5.3 розглянуто задачу знаходження відстані від функцій до простору односторонньо квазінеперервних функцій.

В підрозділі 5.4 вивчається задача про знаходження відстані до простору $K_{x_{0}}(X)$ квазінеперервних в точці $x_{0}$ функцій $f:X\rightarrow \mathbb{R}$. Зокрема, було встановлено наступні результати, в яких використовується поняття граничної множини.

\textbf{Означення} Нехай $X$ і $Y$~--- топологічні простори, $D\subseteq X$, $x_{0}\in \overline{D}$, $\mathcal{U}_{x_{0}}$~--- система всіх околів точки $x_{0}$ в $X$ і $f:D\rightarrow Y$~--- відображення. Перетин $C(f,x_{0})=\bigcap\limits_{U\in \mathcal{U}_{x_{0}}}^{}\overline{f(U\cap D)}$ називається граничною множиною відображення $f$ у точці $x_{0}$.

\textbf{Теорема 5.4.1.} Нехай $X$~--- топологічний простір, $x_{0}\in X$, $\{ x_{0} \}$~--- замкнена множина в $X$, $x_{0}\in \overline{X\setminus \{ x_{0} \}}$, $f:X\rightarrow \mathbb{R}$~--- деяка функція і $C(\dot{f},x_{0})$~--- гранична множина функції $\dot{f}=f|_{X\setminus \{x_{0}\}}:X\rightarrow \mathbb{R}$ зі значеннями в розширеній числовій прямій $\overline{\mathbb{R}}=\mathbb{R}\cup \{+\infty,-\infty\}$. Тоді:


а). Якщо $D(f)\subseteq\{ x_{0} \}$ і $f(x_{0})\in C(\dot{f},x_{0})$, то $f\in K_{x_{0}}(X)$;

б). Якщо $f(x_{0})\notin C(\dot{f},x_{0})$, то $f\notin K_{x_{0}}(X)$.

Позначимо символом $\rho(\xi,A)$~--- відстань від точки $\xi$ до множини $A$ на числовій прямій $\mathbb{R}$.

\textbf{Теорема 5.4.2.} Нехай $X$~--- топологічний простір з першою аксіомою зліченності, $x_{0}$~--- неізольована точка в $X$, для якої множина $\{ x_{0} \}$ замкнена, і $f:X\rightarrow \mathbb{R}$~--- обмежена функція, для якої $D(f)\subseteq\{ x_{0} \}$, та $\dot{f}=f|_{X\setminus \{x_{0}\}}$. Тоді: $$d(f,K_{x_{0}}(X))=\frac{1}{2}\rho(f(x_{0}),C(\dot{f},x_{0}))$$.








\vspace{0.7cm}
\bigskip
\centerline {\bf  ВИСНОВКИ}

В дисертації застосовувалися методи
загальної теорії функцій.

У дисертаційній
роботі отримані такі нові наукові результати:

1) досліджено питання про існування проміжної афінної функції для пари функцій $(g,h)$, де $g$~--- вгнута, $h$~--- опукла, та $g(x)\leq h(x)$ на векторному просторі $X$;

2) доведено існування проміжної кусково лінійної функції з даним значенням для пари функцій $(g,h)$, де $g$~--- напівнеперервна зверху, $h$~--- знизу, та $g(x)< h(x)$ на відрізку;

3) доведено існування проміжної нескінченно диференційовної функції з даним значенням для пари функцій $(g,h)$, де $g$~--- напівнеперервна зверху, $h$~--- знизу, та $g(x)< h(x)$ на відрізку, невиродженому проміжку та замкненому паралелепіпеді в $\mathbb{R}^{n}$;

4) доведено існування строго проміжної нескінченно диференційовної функції $f:X\rightarrow \mathbb{R}$ для строгих пар Гана, заданих на сепарабельних гільбертових просторах з допомогою диференційовних розбиттів одиниці, вписаних в задані відкриті покриття;

5) побудовано строго проміжну диференційовну функцію $f:X\rightarrow \mathbb{R}$ для строгих пар Гана, заданих на асплундових просторах з допомогою диференційовних розбиттів одиниці на цих просторах;

6) отримано аналог теореми Гана для певних многозначних відображень з топологічного простору $X$ в $\mathbb{R}$;


7) введено поняття нарізної пари Гана на добутку просторів $X$ та $Y$ і встановлено, що для існування проміжної нарізно неперервної функції $f:X\times Y \rightarrow \mathbb{R}$ необхідною і достатньою умовою є нормальність простору $(X\times Y,\mathcal{C})$, де $\mathcal{C}$~--- хрест-топологія на добутку топологічних просторів;

8) для кожної напівнеперервної знизу функції $\varphi:[a,b]\rightarrow [0,+\infty)$  і довільного відрізка $[c,d]$ побудовано нарізно неперервну функцію $f:[a,b]\times[c,d]\rightarrow \mathbb{R}$, таку, що нульове найкраще наближення $E_{0}(f^{n})=\varphi(x)$ на $[a,b]$;

9) знайдено рівномірну відстань від довільної функції до простору неперервних на $X$ функцій для нормального простору $X$;

10) знайдено відстань до простору квазінеперервних в неізольованій точці $x_{0}$, функцій заданих на топологічному просторі $X$ з першою аксіомою зліченності для обмеженої функції $f:X\rightarrow \mathbb{R}$, у якої $D(f)\subseteq \{x_{0}\}$.

\vspace{0.3cm}






%\newpage
\vspace{0.7cm}
\bigskip
\bigskip
\centerline{\bf СПИСОК ОПУБЛІКОВАНИХ ПРАЦЬ}

\centerline{\bf ЗА ТЕМОЮ ДИСЕРТАЦІЇ}


%\vspace{0.3cm}


1. Маслюченко В. К., Мельник В. С. Про рівномірне відхилення від
простору неперервних функцій // Збірник праць Ін-ту математики НАН України ~--- Київ: Інститут математики НАН України, 2014. ~--- №1  ~--- \textbf{Т.XI.} ~--- С.158-166.
http://trim.imath.kiev.ua/index.php/trim/issue/view/3


2. Маслюченко В. К., Мельник В. С. Про відстань до множин квазінеперервних у точці функцій // Бук. мат. журн.  ~---  2014. ~--- \textbf{2} (2-3). ~--- С. 154-161.
http://www.bmj.chv.ua/issue.php?i=4


3. Маслюченко В.К., Мельник В.С. Теореми про проміжну афінну функцію для опуклої і вгнутої функцій // Бук. мат. журн. ~--- 2016. ~--- \textbf{4},№1. ~--- С.110-116.
http://www.bmj.chv.ua/issue.php?i=9


4. Маслюченко В.К., Маслюченко О.В., Мельник В.С. Існування проміжних кусково лінійних та нескінченно диференційовних функцій // Бук. мат. журн. ~--- 2016. ~--- \textbf{4},№2-3. ~--- С.93-100.
http://www.bmj.chv.ua/issue.php?i=10


5. Волошин Г.А., Маслюченко В.К., Мельник В.С. Пари Гана і нульова обернена задача // Мат. студії.~--- 2017. ~--- \textbf{48}, №1. ~--- С. 74-81.


6. Маслюченко В.К., Мельник В.С. Побудова проміжних диференційовних функцій // Укр. мат. журн. ~--- 2018. ~--- \textbf{70}, № 5. ~--- С. 672-681.

7. Мельник В. Знаходження відхилень від функцій першшого класу Бера до простору неперервних функцій // Матеріали студ. наук. конф. ЧНУ. 17-18 травня 2011. Фіз.-мат. науки.~--- Чернівці: ЧНУ, 2011.~--- С.409-410.

8. Мельник. В. Про відстань до множини квазінеперервних або ледь неперервних у нулі функцій // Матеріали студ. наук. конф. ЧНУ. 5-6 квітня 2012 Фіз.-мат. науки.~--- Чернівці: ЧНУ, 2012.~--- С.341-342.


9. Мельник. В. Навколо теореми Гана-Д'єдонне-Тонґа-Катетова // Матеріали
студ. наук. конф. ЧНУ.
17-19 квітня 2013 Фіз.-мат. науки.~--- Чернівці: ЧНУ, 2013.~--- С.441-442.

10. V. Maslyuchenko, V.Mel'nyk. Uniform distance to the space of continuous functions // Міжн. конф. "Комплексний аналіз, теорія потенціалу та її застосування"  19 - 23 серпня 2013. - Київ; Інститут математики НАН України, 2013.



11. Маслюченко В. К., Мельник В. С. Про рівномірну відстань до різних функціональних класів // Тези доповідей IV Міжнародної Ганської конференції ~--- Чернівці: Чернівецький національний університет імені Юрія Федьковича, 2014. ~--- С.110-113.

12. Маслюченко В. К., Мельник В. С. Про модифікацію однієї функції Гана // Наукова конференція, присвячена 100-річчю від дня народження К.М. Фішмана та М.К. Фаге. Тези доповідей. ~--- Чернівці: ЧНУ, 2015. ~--- С.68-69.
	
13. Маслюченко В.К, Мельник В.С.  Теореми про проміжну функцію для опуклих функцій// Сучасні проблеми теорії ймовірностей та математичного аналізу: Всеукраїнська наукова конференція, Ворохта 24-27 лютого 2016. (тези доповідей), ~--- Івано-Франківськ: ДВНЗ "Прикарпатський національний університет імені Василя Стефаника", 2016. ~--- С.98-100.

14. Vasyl Melnyk, Volodymyr Maslyuchenko The development of Hahn's theorem on an intermediate function//  International Conference dedicated to the 120-th anniversary of Kazimierz Kuratowski ~--- 27 September - 1 October, 2016.  ~--- P. 36-37.



15. Маслюченко В.К, Мельник В.С.  Про побудову диференційовних проміжних функцій // Сучасні проблеми теорії ймовірностей та математичного аналізу:
Всеукраїнська наукова конференція, тези доповідей. Ворохта, 22 - 25 лютого
2017 р. ~--- Івано-Франківськ: ДВНЗ "Прикарпатський національний
університет імені Василя Стефаника", 2017. ~--- С. 138.

16. Маслюченко В.К, Мельник В.С. Асплундові простори та проміжні диференційовні функції // Міжнародна конференція "Теорія наближення функцій та її застосування" присвячена 75-річчю з дня народження члена-кореспондента НАН України, професора О.І. Степанця (1942-2007)
28 травня - 3 червня 2017 року, Словянськ, Україна: Тези доповідей. ~--- Словянськ: Донбаський державний педагогічний університе, 2017. ~--- С. 104.

17.  Melnyk V.S., On the intermediate multivalued function // Doctoral summerschool on Evolutionary Computing in Optimization and Data Mining JUNE 19 - 22, 2017, Alexandru Ioan Cuza University of Iasi, Romania. https://profs.info.uaic.ro/~summerschool/summerschool/public/

18. Маслюченко В. К., Мельник В. С. Аналог теореми Гана для диференційовних за Фреше функцій // XII-та літня школа АТА, ~--- 10-23 липня 2017 р. ~--- С. 13-14.

19. V. Maslyuchenko, V.Mel'nyk On the intermediate separately continuous function // International Conference in Functional Analysis dedicated to the 125th anniversary of Stefan Banach Lviv, Ukraine, ~--- 18-23 September 2017

20. Маслюченко В. К., Мельник В. С. Про проміжну многозначу функцію // Матеріали міжвузівського наукового семінару Прикладні задачі та ІТ-технології, присвячений 100-річчю з дня народження професора Василя Павловича Рубаника (1917~--- 1993) і 55-річчю кафедри прикладної математики та інформаційних технологій. ~--- С. 66-68.

%до кінця



\newpage
\begin{center}
АНОТАЦІЯ
\end{center}

\textbf{\emph{Мельник В.С.} Рівномірні відстані до різних функціональних класів та розвиток теореми Гана про проміжну функцію.} -- Рукопис.

Дисертація на здобуття наукового ступеня кандидата фізико-математичних наук (доктора філософії) за спеціальністю 01.01.01 -- "Математичний аналіз" (01 - Математика). - Чернівецький національний університет імені Юрія Федьковича, Чернівці, 2019.

\vspace{\baselineskip}
Дисертація присвячена дослідженню можливості перенесення результатів теореми Гана-Д'єдонне-Тонґа-Катетова про існування проміжної неперервної функції для двох напівнеперервних зверху та знизу функцій $g,h:X\rightarrow \mathbb{R}$, таких, що $g(x)\leq h(x)$ на $X$, на інші класи функцій. Крім того, знаходяться рівномірні відстані від функцій до різних функціональних класів.

В дисертації введені поняття пари Гана, тобто пари $(g,h)$ напівнеперервних відповідно зверху і знизу функцій $g,h:X\rightarrow \mathbb{R}$ на топологічному просторі $X$, таких, що $g(x)\leq h(x)$ на $X$, та строгої пари Гана, коли $g(x)<h(x)$ на $X$.

Побудовано проміжну афінну функцію для опуклої та вгнутої функцій $g,h:X\rightarrow \mathbb{R}$, $g(x)\leq h(x)$, заданих на векторному просторі $X$.

Сконструйовано строго проміжні диференційовні функції для строгих пар Гана на відрізках, паралелепіпедах в $\mathbb{R}^{n}$, гільбертових та асплундових просторах.

З допомогою хрест-топології розв'язана задача про побудову проміжної нарізно неперервної функції для нарізно напівнеперервних пар Гана на добутках топологічних просторів, а також проміжної многозначної функції для многозначних пар Гана.

Завершується дисертація розв'язанням задачі знаходження рівномірної відстані $d(f,C(X))$ та $d(f,K_{x_{0}}(X))$ від функцій, заданих на топологічному просторі $X$ до просторів $C(X)$ неперервних та $K_{x_{0}}(X)$ квазінеперервних у точці $x_{0}$ функцій.


 \textbf{Ключові слова:}

Пара Гана, строга пара Гана, проміжна функція, строго проміжна функція, неперервні функції, нарізно неперервні відображення, асплундові простори, квазінеперервні функції, найкращі наближення, нормальні простори, паракомпактні простори, розбиття одиниці.

\bigskip
\begin{center}
АННОТАЦИЯ
\end{center}

\textbf{\emph{Мельник В.С.} Равномерные расстояния до разных функциональных классов и развитие теоремы Гана о промежной функции.} -- Рукопись.

Диссертация на соискание научного степени кандидата физико-математических наук (доктора философии) по специальности 01.01.01 -- "Математический анализ" (01 - Математика). - Черновицкий национальний университет им. Юрия Федьковича, Черновцы, 2019.

\vspace{\baselineskip}
Диссертация посвящена исследованию возможности переноса результатов теоремы Гана-Дьедонне-Тонга-Катетова о существовании промежуточной непрерывной функции для двух полунепрерывных сверху и снизу функций $ g, h: X\rightarrow \mathbb{R} $ таких, что $ g (x) \leq h (x) $ в $ X $ на другие классы функций. Кроме того, находятся равномерные расстояния от функций к различным функциональным классам.

В диссертации введено понятие пары Гана, то есть, пары $ (g, h) $ полунепрерывных соответственно сверху и снизу функций $ g, h: X \rightarrow \mathbb{R} $ на топологическом пространстве $ X $, таких, что $ g (x) \leq h (x) $ в $ X $ и строгие пары Гана, когда $ g (x) <h (x) $ в $ X $.

Построено промежуточную аффинную функцию для выпуклой и вогнутой функций $ g, h: X \rightarrow \mathbb{R} $, $ g (x) \leq h (x) $, заданных на векторном пространстве $ X $.

Сконструированы строго промежуточные дифференцируемые функции для строгих пар Гана на отрезках, параллелепипедах в $ \mathbb{R}^{n} $, асплундових и гильбертовых пространствах.

С помощью крест-топологии решена задача о построении промежуточной врозь непрерывных функций для врозь полунепрерывных пар Гана на произведениях топологических пространств, а также промежуточной многозначной функции для многозначных пар Гана.

Завершается диссертация решением задач нахождения равномерных расстояний $ d (f, C (X)) $ и $ d (f, K_{x_{0}}(X)) $ от функций, заданных на топологическом пространстве $ X $ до пространств $ C (X) $ непрерывных и $ K_{x_{0}}(X) $ квазинеперерывных в точке $x_{0}$ функций.



 \textbf{Ключевые слова:}


Пара Гана, строгая пара Гана, промежуточная функция, строго промежуточная функция, непрерывная функция, врозь непрерывные отображения, асплундовые пространства, квазинепрерывные функции, наилучшее приближение, нормальное пространство, паракомпактные пространства, разбиение единицы.



\begin{center}
ABSTRACT
\end{center}

\textbf{\emph{Melnyk V.} Uniform distances to different functional classes and development of Hahn's theorem on intermediate function.} Manuscript.

\vspace{0.1 cm}

A dissertation for the degree  of Candidate of physical and mathematical sciences (Doctor of philosophy) of speciality 01.01.01 -- "Mathematical analysis" (01 - Mathematics). -- Yuriy Fedkovych Chernivtsi National University, Chernivtsi, 2019.

\vspace{\baselineskip}
The Dissertation is dedicated to investigation of possibility of transfer of the results of Hahn-Dieudonne-Tong-Katetov theorem on intermediate continuous function for two semicontinuous lower and upper respectively functions $g,h:X\rightarrow \mathbb{R}$ such, that $g(x)\leq h(x)$ on $X$ for other classes of functions. Also, uniform distances from functions to different classes of functions.

The Hahn's pairs are introduced in this dissertation, that being the pairs $(g,h)$ of semicontinuous respectively upper and lower functions $g,h:X\rightarrow \mathbb{R}$ on topological space $X$, such, that $g(x)\leq h(x)$ on $X$, and strict Hahn's pairs, when $g(x)<h(x)$ on $X$.

We constructed intermediate affine function for convex and concave functions $g,h:X\rightarrow \mathbb{R}$, $g(x)\leq h(x)$, given on vector space $X$.

Also we have constructed strictly intermediate differentiable functions for strict Hahn's pairs on segments, parallelepipeds in $\mathbb{R}^{n}$, asplund and Hilbert spaces.

With the help of cross topology there were received a number of results on transfer of Hahn's result to the case of separately continuous functions on products of topological spaces, and also for multivalued Hahn's pairs.

Last part of dissertation is dedicated to the problem of finding of uniform distance $d(f,C(X))$ and $d(f,K_{x_{0}}(X))$ from functions, given on topological space $X$, to spaces $C(X)$ of continuous and $K_{x_{0}}(X)$ of quasicontinuous in $x_{0}$ functions.


 \textbf{Key words:}

Hahn's pair, strict Hahn's pair, intermediate function, strictly intermediate function, continuous function, separately continuous maps, asplund spaces, quasicontinuous functions, best approximation, normal spaces, paracompact spaces, partition of unity.



\newpage

\bigskip

\vspace*{\fill}


\centerline{Підписано до друку 26.11.2018. Формат 60x84/16}

\centerline{Папір офсетний. Друк різографічний. Ум.-друк. арк. 0,9.}

\centerline{Обл.-вид. арк. 0,9. Тираж 100. Зам. А-012.}

\centerline{Видавництво та друкарня Чернівецького національного університету}

\centerline{імені Юрія Федьковича}

\centerline{58002, Чернівці, вул. Коцюбинського, 2}

\centerline{\textit{e-mail: \underline{ruta@chnu.edu.ua}}}
\bigskip

\centerline{\textit{Свідоцтво суб'єкта видавничої справи ДК №891 від 08.04.2002 р.}}



\end{document}
