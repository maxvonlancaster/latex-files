\newpage
\begin{center}
АНОТАЦІЯ
\end{center}

\textbf{\emph{Мельник В.С.} Рівномірні відстані до різних функціональних класів та розвиток теореми Гана про проміжну функцію.} -- Рукопис.

Дисертація на здобуття наукового ступеня кандидата фізико-математичних наук (доктора філософії) за спеціальністю 01.01.01 -- "Математичний аналіз" (01 - Математика). - Чернівецький національний університет імені Юрія Федьковича, Чернівці, 2019.

\vspace{\baselineskip}
Дисертація присвячена дослідженню можливості перенесення результатів теореми Гана-Д'єдонне-Тонґа-Катетова про існування проміжної неперервної функції для двох напівнеперервних зверху та знизу функцій $g,h:X\rightarrow \mathbb{R}$, таких, що $g(x)\leq h(x)$ на $X$, на інші класи функцій. Крім того, знаходяться рівномірні відстані від функцій до різних функціональних класів.

В дисертації введені поняття пари Гана, тобто пари $(g,h)$ напівнеперервних відповідно зверху і знизу функцій $g,h:X\rightarrow \mathbb{R}$ на топологічному просторі $X$, таких, що $g(x)\leq h(x)$ на $X$, та строгої пари Гана, коли $g(x)<h(x)$ на $X$.

Побудовано проміжну афінну функцію для опуклої та вгнутої функцій $g,h:X\rightarrow \mathbb{R}$, $g(x)\leq h(x)$, заданих на векторному просторі $X$.

Сконструйовано строго проміжні диференційовні функції для строгих пар Гана на відрізках, паралелепіпедах в $\mathbb{R}^{n}$, гільбертових та асплундових просторах.

З допомогою хрест-топології розв'язана задача про побудову проміжної нарізно неперервної функції для нарізно напівнеперервних пар Гана на добутках топологічних просторів, а також проміжної многозначної функції для многозначних пар Гана.

Завершується дисертація розв'язанням задачі знаходження рівномірної відстані $d(f,C(X))$ та $d(f,K_{x_{0}}(X))$ від функцій, заданих на топологічному просторі $X$ до просторів $C(X)$ неперервних та $K_{x_{0}}(X)$ квазінеперервних у точці $x_{0}$ функцій.


 \textbf{Ключові слова:}

Пара Гана, строга пара Гана, проміжна функція, строго проміжна функція, неперервні функції, нарізно неперервні відображення, асплундові простори, квазінеперервні функції, найкращі наближення, нормальні простори, паракомпактні простори, розбиття одиниці. 